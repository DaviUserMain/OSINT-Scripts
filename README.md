# Automation Scripts for OSINT

This repository contains a collection of Python scripts designed to automate repetitive tasks during OSINT investigations. The goal is to create simple and effective tools to speed up information gathering.

---

## 🚀 How to Use

1. Clone the repository to your local machine:
```bash
git clone [https://github.com/DaviLuanMain/OSINT-Scripts.git](https://github.com/DaviLuanMain/OSINT-Scripts.git)
```
2. Navigate to the repository folder:
```bash
cd OSINT-Scripts
```
3. Install the required dependencies (if any):
```bash
pip install -r requirements.txt
```
4. Run the desired script. Example:
```bash
python3 username_checker.py example_user
```

---

## 📜 Script List

| File Name | Language | Description |
| -------------------------- | :-------: | --------------------------------------------------------------------- |
| `username_checker.py` | Python | Searches for a username in a predefined list of social networks. |
| `exif_extractor.py` | Python | Extracts EXIF ​​metadata (camera, date, location) from an image file. |
| `subdomain_finder.py` | Python | Finds subdomains of a target domain using public APIs. |
